# signal-dev — meta-repo

This folder is a container for independent repos, not a single project. Each subfolder is its own repo with its own `.git/`.

Whenever a new git repo subfolder is added here, add its name to `.gitignore` so the meta-repo never tracks nested repo contents.