# Server-side only — never exposed to browser (no NEXT_PUBLIC_ prefix).
# Fetches happen in server components or the route handler at app/api/signal/*.
SIGNAL_API_URL=https://<your-function-app>.azurewebsites.net
SIGNAL_API_KEY=<same-as-backend>
